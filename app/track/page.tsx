"use client"

import { useEffect, useState } from "react"
import Link from "next/link"
import { ArrowLeft, Check, Circle, Clock3, Search } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Badge } from "@/components/ui/badge"
import { Alert, AlertDescription } from "@/components/ui/alert"

const demoTimeline = [{ label: "Complaint registered", detail: "Your complaint was received and assigned to the local queue.", done: true }, { label: "Under review", detail: "A ward officer is reviewing the issue and preparing the next action.", done: true }, { label: "Field action", detail: "The responsible department will update this step when work begins.", done: false }, { label: "Resolved", detail: "You will see the resolution note here.", done: false }]

export default function TrackComplaintPage() {
  const [code, setCode] = useState("")
  const [complaint, setComplaint] = useState<any>(null)
  const [error, setError] = useState("")
  const [loading, setLoading] = useState(false)
  useEffect(() => { const value = new URLSearchParams(window.location.search).get("code"); if (value) setCode(value) }, [])
  const track = async () => { if (!code.trim()) { setError("Enter your private tracking code."); return }; setLoading(true); setError(""); try { const response = await fetch(`${process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000"}/api/v1/complaints/track/${encodeURIComponent(code.trim())}`); if (!response.ok) throw new Error(); setComplaint(await response.json()) } catch { setError("We could not find that complaint. Check the code and try again.") } finally { setLoading(false) } }
  return <main className="min-h-screen bg-background"><header className="border-b border-border/70"><div className="mx-auto flex max-w-5xl items-center px-5 py-4 lg:px-8"><Link href="/"><Button variant="ghost" size="sm"><ArrowLeft data-icon="inline-start" />Back</Button></Link></div></header><section className="mx-auto max-w-3xl px-5 py-10 lg:px-8 lg:py-16"><div className="mb-8"><p className="text-sm font-medium text-primary">Complaint tracking</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">See what happens next.</h1><p className="mt-3 text-lg leading-7 text-muted-foreground">Use the private tracking code you received after submitting your complaint.</p></div><Card><CardHeader><CardTitle>Enter tracking code</CardTitle><CardDescription>No email, phone number, or OTP needed.</CardDescription></CardHeader><CardContent className="flex flex-col gap-4 sm:flex-row"><Input value={code} onChange={(event) => setCode(event.target.value.toUpperCase())} onKeyDown={(event) => { if (event.key === "Enter" && !event.nativeEvent.isComposing && event.keyCode !== 229) track() }} placeholder="Example: NS-4K8P2M" className="h-11 font-mono uppercase" /><Button onClick={track} disabled={loading} className="h-11 sm:w-36"><Search data-icon="inline-start" />{loading ? "Checking" : "Track"}</Button></CardContent></Card>{error && <Alert variant="destructive" className="mt-5"><AlertDescription>{error}</AlertDescription></Alert>}{complaint && <Card className="mt-6"><CardHeader><div className="flex flex-wrap items-start justify-between gap-3"><div><CardTitle>{complaint.category || "Civic complaint"}</CardTitle><CardDescription className="mt-1 font-mono">{complaint.complaint_id}</CardDescription></div><Badge>{complaint.status || "Under review"}</Badge></div></CardHeader><CardContent className="flex flex-col gap-7"><div className="grid gap-4 rounded-xl bg-secondary p-4 sm:grid-cols-3"><div><p className="text-xs text-muted-foreground">Area queue</p><p className="mt-1 font-medium">{complaint.ward || "Assigned ward"}</p></div><div><p className="text-xs text-muted-foreground">Department</p><p className="mt-1 font-medium">{complaint.department || "Local civic team"}</p></div><div><p className="text-xs text-muted-foreground">Last updated</p><p className="mt-1 font-medium">{complaint.updated_at ? new Date(complaint.updated_at).toLocaleDateString() : "Today"}</p></div></div><div><h2 className="mb-5 font-semibold">Resolution timeline</h2><div className="flex flex-col gap-0">{(complaint.timeline || demoTimeline).map((item: any, index: number) => <div key={item.label} className="flex gap-4"><div className="flex flex-col items-center"><span className={`grid size-8 place-items-center rounded-full border ${item.done ? "border-primary bg-primary text-primary-foreground" : "border-border bg-background text-muted-foreground"}`}>{item.done ? <Check className="size-4" /> : index === 2 ? <Clock3 className="size-4" /> : <Circle className="size-3" />}</span>{index < (complaint.timeline || demoTimeline).length - 1 && <span className={`my-1 min-h-10 w-px ${item.done ? "bg-primary/40" : "bg-border"}`} />}</div><div className="pb-6"><p className="font-medium">{item.label}</p><p className="mt-1 text-sm leading-6 text-muted-foreground">{item.detail}</p></div></div>)}</div></div></CardContent></Card>}</section></main>
}
